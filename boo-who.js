function booWho(bool) {
  //What is the new fad diet for ghost developers? The Boolean.
  if (typeof(bool)) === "boolean") {
    //checks if typeof returns 'boolean'
    return true;
  } else {
    //type is not a boolean and returns false
    return false;
  }
}

booWho(null);
